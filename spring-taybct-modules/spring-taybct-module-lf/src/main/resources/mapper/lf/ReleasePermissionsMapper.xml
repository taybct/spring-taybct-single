<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.mangocrisp.spring.taybct.module.lf.mapper.ReleasePermissionsMapper">

    <resultMap id="BaseResultMap" type="io.github.mangocrisp.spring.taybct.module.lf.domain.ReleasePermissions">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="releaseId" column="release_id" jdbcType="BIGINT"/>
        <result property="deptId" column="dept_id" jdbcType="BIGINT"/>
        <result property="userId" column="user_id" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id
        ,release_id,dept_id,user_id
    </sql>

    <insert id="publishPermission">
        insert into lf_release_permissions(id, release_id, dept_id, user_id)
        select #{id}, #{releaseId}, dept_id, user_id
        from lf_design_permissions
        where design_id = #{designId}
    </insert>

</mapper>
